import styled from "styled-components";

const Wrapper = styled.div`
.record-section {
    flex-direction: row;
    gap: 20px;
    align-items: center;
}

span.sensor-icon, span.status {
    font-size: 25px;
}
h5{
    margin-bottom: 0;
}
`

export default Wrapper